# Uncomment the next line to define a global platform for your project
platform :ios, "12.2"

target "practical-skill-test-ios" do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for practical-skill-test-ios
  pod "Firebase/Core"
  pod "Firebase/Auth"

  target "practical-skill-test-iosTests" do
    inherit! :search_paths
    # Pods for testing
  end

  target "practical-skill-test-iosUITests" do
    inherit! :search_paths
    # Pods for testing
    pod "Firebase/Core"
    pod "Firebase/Auth"
  end
end
